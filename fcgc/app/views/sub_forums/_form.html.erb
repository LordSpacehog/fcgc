<%= form_for(@sub_forum) do |f| %>
  <% if @sub_forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_forum.errors.count, "error") %> prohibited this sub_forum from being saved:</h2>

      <ul>
      <% @sub_forum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
